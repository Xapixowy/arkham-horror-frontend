import {pl} from 'date-fns/locale';
import {DefaultOptions} from 'node_modules/date-fns/_lib/defaultOptions';

export const DATE_FNS_CONFIG: DefaultOptions = {
  locale: pl,
  weekStartsOn: 1,
};
